[project]
name = "reasonforge"
version = "0.1.0"
description = "Deterministic math tools for small language models via MCP"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "gradio>=5.0",
    "mcp[cli]>=1.26.0",
    "sympy>=1.13.0",
    "tiktoken>=0.8.0",
]
